/**
 * @description Page objects for form submission page.
 * @author Sindhu Koti
 */

const formPageObject = {
     nameInput : '[name=namec]',
     streetInput : '[name=street]', 
     countrySelect : 'select',
     emailInput : '[name=email]', 
     principalInvestorRadio : '[type=radio]' , 
     researchApplicantChecbox : '[type=checkbox]',
     submit : '[type=submit]'

}

export default formPageObject ;